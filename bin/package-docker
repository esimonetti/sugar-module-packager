#!/bin/bash
if [ -z $1 ]
then
    echo Use ./vendor/bin/package-docker \[version\]
else
    docker run -it -v ${PWD}:/usr/src/packager -w /usr/src/packager esimonetti/sugarmodulepackager:v0.1 sh -c "./vendor/bin/package $1"
fi
